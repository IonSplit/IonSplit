'use strict';

var runnerDOM = {
  add: function(DOM) {
    // FIXME:
  },
  remove: function(selector) {
    // FIXME:
  },
  update: function(selector, DOM) {
    // FIXME:
  }
}

module.exports = runnerDOM;
